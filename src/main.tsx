import React from 'react'
import ReactDOM from 'react-dom/client'
import FedererDashboard from '../Darth_Federer.tsx'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FedererDashboard />
  </React.StrictMode>,
)
